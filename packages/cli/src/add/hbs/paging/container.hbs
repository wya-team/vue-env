<template>
	<set-title title="{{title}}" style="padding: 24px">
		<{{project}}-filter />
		{{#if isBasic}}
		<{{project}}-list />
		{{else}}
		<{{project}}-tabs-list />
		{{/if}}
	</set-title>
</template>

<script>
import Filter from '@components/{{moduleName}}/{{childName}}/filter';
{{#if isBasic}}
import List from '@components/{{moduleName}}/{{childName}}/list';
{{else}}
import TabsList from '@components/{{moduleName}}/{{childName}}/tabs-list';
{{/if}}
import navigator from '@extends/mixins/navigator';

export default {
	name: '{{componentName}}',
	components: {
		"{{project}}-filter": Filter,
		{{#if isBasic}}
		"{{project}}-list": List,
		{{else}}
		"{{project}}-tabs-list": TabsList,
		{{/if}}
	},
	mixins: [navigator('{{mutationPrefix}}')]
};
</script>